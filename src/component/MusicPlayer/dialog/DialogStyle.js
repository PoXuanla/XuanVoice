import styled from '@emotion/styled'
import { Box } from '@mui/material'

export const MusicImgStyle = styled(Box)(({ theme }) => ({
  width: 200,
  height: 200
}))
export const MusicInfoStyle = styled(Box)(({ theme }) => ({
  display: 'flex'
}))
